import React from 'react';
import 'bootstrap/dist/css/bootstrap.css';

// TODO: Create MovieCard to list Movies
const MovieCard = (movie) => {
    return (
        <div></div>
    )
};

export default MovieCard;
